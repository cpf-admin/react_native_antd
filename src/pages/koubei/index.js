import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import PopoverEle from '../../components/popover';

export default class Koubei extends React.Component {
  constructor() {
    super(...arguments);
  }
  
  render() {
    return (
      <PopoverEle />
    );
  }
}

const styles = StyleSheet.create({
  
});
